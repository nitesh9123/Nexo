
  # Service-Fixing Platform App

  This is a code bundle for Service-Fixing Platform App. The original project is available at https://www.figma.com/design/PSd3SQUTtC0yQOBo2wIoE8/Service-Fixing-Platform-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  